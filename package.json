{
	"name": "tabby-claude-status-monorepo",
	"version": "0.0.0",
	"private": true,
	"description": "Repo-root tooling: Biome for lint+format across plugin/ and web/.",
	"scripts": {
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"format": "biome format --write .",
		"lint": "biome lint ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.4"
	}
}
